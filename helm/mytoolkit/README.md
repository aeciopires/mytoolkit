# mytoolkit

![Version: 1.0.0](https://img.shields.io/badge/Version-1.0.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.0.0](https://img.shields.io/badge/AppVersion-1.0.0-informational?style=flat-square)

A suite of developer utilities exposed as a web app, REST API, and CLI.

**Homepage:** <https://github.com/aeciopires/mytoolkit>

## Maintainers

| Name | Email | Url |
| ---- | ------ | --- |
| Aecio dos Santos Pires |  | <https://aeciopires.com/> |

## Installing

Access a Kubernetes cluster.

If you have [kind](https://kind.sigs.k8s.io/) installed, create a local cluster and install the helm chart using the following commands:

```bash
make helm-lint
make kind-load
make helm-install
```

Or directly with Helm:

```bash
helm upgrade --install mytoolkit -f helm/mytoolkit/values.yaml helm/mytoolkit --namespace mytoolkit --create-namespace
```

Create a port-forward with the follow command:

```bash
kubectl port-forward svc/mytoolkit 8080:8080 -n mytoolkit
```

Open the browser and access the URL: http://localhost:8080

## Uninstalling

```bash
helm uninstall mytoolkit --namespace mytoolkit
```

Or with the Makefile:

```bash
make helm-uninstall
```

## Values

This table is generated by [helm-docs](https://github.com/norwoodj/helm-docs) from the `# --` comments in `values.yaml` — run `make helm-docs` after changing `values.yaml`, don't edit this file by hand.

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| affinity | object | `{}` | Affinity rules for pod scheduling |
| autoscaling.enabled | bool | `false` | Enable a HorizontalPodAutoscaler |
| autoscaling.maxReplicas | int | `5` | Maximum replica count |
| autoscaling.minReplicas | int | `1` | Minimum replica count |
| autoscaling.targetCPUUtilizationPercentage | int | `80` | Target average CPU utilization percentage |
| env.logLevel | string | `"info"` | Log level: debug, info, warn, error (MYTOOLKIT_LOG_LEVEL) |
| extraEnv | list | `[]` | Extra environment variables appended verbatim (list of name/value or valueFrom entries) |
| extraManifests | list | `[]` | Extra arbitrary Kubernetes manifests to deploy within the release |
| fullnameOverride | string | `""` | Overrides the chart's computed fullname (release-qualified) |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy |
| image.repository | string | `"mytoolkit"` | Container image repository |
| image.tag | string | `""` | Image tag; defaults to the chart's appVersion if empty |
| imagePullSecrets | list | `[]` | Image pull secrets for private registries |
| ingress.annotations | object | `{}` | Extra Ingress annotations |
| ingress.className | string | `""` | IngressClass name |
| ingress.enabled | bool | `false` | Enable an Ingress resource |
| ingress.hosts | list | `[{"host":"mytoolkit.local","paths":[{"path":"/","pathType":"Prefix"}]}]` | Ingress hosts |
| ingress.tls | list | `[]` | Ingress TLS configuration |
| livenessProbe | object | `{"failureThreshold":3,"initialDelaySeconds":10,"path":"/healthz","periodSeconds":10,"successThreshold":1,"timeoutSeconds":5}` | Health check continuously |
| livenessProbe.failureThreshold | int | `3` | When a probe fails, Kubernetes will try failureThreshold times before giving up. Giving up in case of liveness probe means restarting the container. In case of readiness probe the Pod will be marked Unready |
| livenessProbe.initialDelaySeconds | int | `10` | Number of seconds after the container has started before liveness |
| livenessProbe.path | string | `"/healthz"` | Path of health check of application |
| livenessProbe.periodSeconds | int | `10` | Specifies that the kubelet should perform a liveness probe every N seconds |
| livenessProbe.successThreshold | int | `1` | Minimum consecutive successes for the probe to be considered successful after having failed |
| livenessProbe.timeoutSeconds | int | `5` | Number of seconds after which the probe times out |
| metrics.serviceMonitor.enabled | bool | `false` | Create a Prometheus Operator ServiceMonitor (requires the CRD to be installed) |
| metrics.serviceMonitor.interval | string | `"30s"` | Scrape interval for the ServiceMonitor |
| metrics.serviceMonitor.labels | object | `{}` | Extra labels on the ServiceMonitor (e.g. to match a Prometheus release label selector) |
| nameOverride | string | `""` | Overrides the chart's computed fullname |
| nodeSelector | object | `{}` | Node selector for pod scheduling |
| pdb | object | `{"enabled":false,"maxUnavailable":"25%"}` | PodDisruptionBudget configuration |
| podAnnotations | object | `{}` | Extra pod annotations, merged with the built-in Prometheus scrape annotations |
| podSecurityContext | object | `{"fsGroup":65532,"runAsNonRoot":true}` | Pod-level securityContext |
| readinessProbe | object | `{"failureThreshold":3,"initialDelaySeconds":10,"path":"/readyz","periodSeconds":10,"successThreshold":1,"timeoutSeconds":5}` | Health check on creation pod |
| readinessProbe.failureThreshold | int | `3` | When a probe fails, Kubernetes will try failureThreshold times before giving up. Giving up in case of liveness probe means restarting the container. In case of readiness probe the Pod will be marked Unready |
| readinessProbe.initialDelaySeconds | int | `10` | Number of seconds after the container has started before readiness |
| readinessProbe.path | string | `"/readyz"` | Path of health check of application |
| readinessProbe.periodSeconds | int | `10` | Specifies that the kubelet should perform a liveness probe every N seconds |
| readinessProbe.successThreshold | int | `1` | Minimum consecutive successes for the probe to be considered successful after having failed |
| readinessProbe.timeoutSeconds | int | `5` | Number of seconds after which the probe times out |
| replicaCount | int | `1` | Number of pod replicas |
| resources | object | `{"limits":{"cpu":"500m","memory":"128Mi"},"requests":{"cpu":"50m","memory":"64Mi"}}` | Pod resource requests/limits |
| secret.data | object | `{}` | Secret data as plain key/value pairs (base64-encoded by the template) |
| secret.enabled | bool | `false` | Create a Secret resource (placeholder for future secret-backed config; disabled by default) |
| securityContext | object | `{"allowPrivilegeEscalation":false,"capabilities":{"drop":["ALL"]},"readOnlyRootFilesystem":true,"runAsNonRoot":true,"runAsUser":65532}` | Container-level securityContext |
| service.port | int | `8080` | Kubernetes Service port |
| service.type | string | `"ClusterIP"` | Kubernetes Service type |
| serviceAccount.annotations | object | `{}` | Extra annotations for the ServiceAccount |
| serviceAccount.create | bool | `true` | Whether to create a ServiceAccount |
| serviceAccount.name | string | `""` | ServiceAccount name; generated if empty |
| tolerations | list | `[]` | Tolerations for pod scheduling |
| updateStrategy | object | `{"rollingUpdate":{"maxSurge":6,"maxUnavailable":0},"type":"RollingUpdate"}` | Update strategy configurations |
