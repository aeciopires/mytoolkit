apiVersion: v1
kind: Service
metadata:
  name: {{ include "mytoolkit.fullname" . }}
  labels:
    {{- include "mytoolkit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mytoolkit.selectorLabels" . | nindent 4 }}
